import 'package:injectable/injectable.dart';

const androidStr = 'android';
const webStr = 'web';
const android = const Environment(androidStr);
const web = const Environment(webStr);

const appPlaystoreLink = "https://play.google.com/store/apps/details?id=com.az.igit.tnp";

const registerLink = "https://home.igitplacements.ac.in/registration";

const version = "2.0.0";

const double maxWidth = 700;